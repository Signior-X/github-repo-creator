export class RepoResponse {
    // The Repo creatin succeeded or not
    status: boolean;

    // The description of the created repo
    description: string;

    // The JSON Error for what error came
    err: string;
}
